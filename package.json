{
  "name": "@flarenetwork/flare-tx-verifier",
  "version": "1.0.3",
  "description": "A CLI tool for verifying Flare C-chain and P-chain transactions.",
  "main": "dist/index.js",
  "files": [
    "/dist"
  ],
  "types": "dist/index.d.ts",
  "bin": "./dist/index.js",
  "homepage": "https://github.com/flare-foundation/flare-tx-verifier",
  "repository": {
    "type": "git",
    "url": "https://github.com/flare-foundation/flare-tx-verifier"
  },
  "scripts": {
    "verify": "npx ts-node ./src/index",
    "test": "npx ts-node ./test/index",
    "build": "rm -rf dist/ && npx tsc -b && cp -r ./build/src ./dist && rm -rf ./build && sed -i '1s;^;#!/usr/bin/env node\\n;' ./dist/index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@flarenetwork/flare-tx-verifier-lib": "^1.0.1",
    "web3-utils": "^4.1.1"
  },
  "devDependencies": {
    "clipboardy": "^2.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
